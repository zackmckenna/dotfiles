[user]
	name = Zack McKenna
	email = zack.mckenna@runpod.io
    # name = Your Name
    # email = your.email@example.com

[core]
    editor = nano
    excludesfile = ~/.gitignore_global

[color]
    ui = auto
    diff = auto
    status = auto
    branch = auto

[push]
    default = current

[pull]
    rebase = false

[alias]
    st = status
    co = checkout
    br = branch
    ci = commit
    unstage = reset HEAD --
    last = log -1 HEAD
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	sync-main = \\!git fetch origin main && git merge origin/main --no-edit && git push
    
[init]
    defaultBranch = main
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[credential "https://github.com"]
	helper = 
	helper = !/opt/homebrew/bin/gh auth git-credential
[credential "https://gist.github.com"]
	helper = 
	helper = !/opt/homebrew/bin/gh auth git-credential
